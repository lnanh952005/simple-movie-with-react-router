import styled from "styled-components"
const MovieStyle = styled.div`

  .swiper-wrapper {
    display: flex;

    .swiper-slide {
      max-width: 300px;
      width: 100%;
    }
  }
`;

export default MovieStyle